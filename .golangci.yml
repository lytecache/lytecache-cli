version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - revive
    - bodyclose
    - sqlclosecheck
    - unconvert

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
